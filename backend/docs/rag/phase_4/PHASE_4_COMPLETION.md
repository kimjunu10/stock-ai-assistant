# Phase 4 완료 보고 — 숫자·용어·혼합 질문

- 완료일: 2026-07-22
- 브랜치: `rag/phase4`
- 범위: 기존 DART·재무 데이터 **읽기 전용** 사용. 정확 숫자(SQL) + 설명(RAG) + 용어 결합.

## 1. 사전 데이터 조사 (읽기 전용, `DATA_SURVEY.md`)
- financials 510 / structured_disclosures 1,524 / disclosures 4,722 / corporate_events 142 / company_profiles 5
- 재무: 5종목 × 2024~2026 × 분기 × **연결(CFS)만**, thstrm_amount는 **원 단위 정수**(단위 컬럼 없음)
- 정정공시: `is_latest`/`correction_status`/`supersedes_rcept_no`로 최신본·정정 전 구분
- 원문(raw_text): success 389건만(나머지 pending=NULL) → 원문 설명 범위 제약
- structured summary_text 거의 전부 존재 → RAG 설명 보완 소스
- 답 가능/불가 경계 문서화(별도재무·2023 이전·용어 시드 필요 등)

## 2. 구현
- **QueryPlan**(`query_plan.py`): 규칙 기반, 혼합 질문 복수 플래그, 종목 결정 우선순위
- **SQL 어댑터**(`facts.py`, 읽기 전용): 재무/구조화공시/정정공시 최신본/용어 조회
  - value_kind(실제/공식/전망), 기간(분기·누적/당기), 연결/별도 라벨
  - 정정: is_latest=true 우선, 정정 전후 페어 조회
- **금융용어**: rag_terms 소량 시드 6개(ADR 등, 데이터로 주입) + 정확일치/별칭/유사 조회
- **통합 QA**(`rag_qa_facts.py`): 숫자 SQL + 문서 RAG + 용어를 **병렬** 실행 후 합성,
  숫자 출처와 설명 출처 **분리** 반환
- 프롬프트: 실제/전망 구분·기간 라벨 유지·정정 최신본 규칙 명시

## 3. 소량 검증 결과 (필수 시험 질문, `trial_result.json`)

| 질문 | plan | 결과 |
|---|---|---|
| 영업이익이 얼마야? | financials+docs | 숫자 12건 조회, 57.23조원(실제 실적, 연결) 답변 |
| 영업이익이 얼마고 왜 늘었어? | financials+docs | 숫자+설명 병렬, "2026년 3분기 기준" 정확 표기 |
| ADR이 뭐야? | terms | **term=ADR**(시드 조회), 초보자 설명 |
| 정정 전과 정정 후 뭐가 바뀌었어? | docs+correction | 최신 정정본 기준 응답, 정정 전을 최신처럼 말하지 않음 |
| 이 공시가 왜 중요해? | docs | 관련 공시 설명(출처 8) |
| 없는 회사 재무 알려줘 | (근거 부족) | 정직하게 "확인 불가" 응답 |

- 모든 케이스 **인용 오류 0**
- 혼합 질문이 숫자 조회(SQL)와 설명 검색(RAG)을 **모두** 사용함 확인
- 용어 조사 접미사 처리("ADR이"→"ADR") 일반 규칙으로 해결

## 3-1. ⚠ 금융용어 사전 현황 (중요 — 미완료 명시)

**용어 검색 구조만 구현했고, 전체 용어사전 전처리·적재는 미완료다.**

- 현재 `rag_terms` 6건은 **시험용 시드**다. `scripts/seed_rag_terms.py`에 손으로 적은
  일반 금융 용어(ADR·영업이익·당기순이익·유상증자·자기주식·정정공시)이며, `source_page`는
  전부 NULL(외부 사전 출처 없음). 기존 금융용어 사전 전체를 전처리·적재한 것이 **아니다.**
- 구현된 것은 **용어 조회 로직**(정확일치→별칭→유사)과 **적재 경로**(rag_terms 스키마)까지다.

### 사용 가능한 원본 용어사전 데이터 조사 (보고만, 적재 안 함)

원본 용어사전 PDF를 조사하고 **출처·이용 근거를 검증**했다. 결과에 따라 대상을 확정했다:

| 파일 | 현재 위치 | 규모 | 출처 검증 | Phase 4 대상 |
|---|---|---|---|---|
| `2026_경제금융용어 800선.pdf` (한국은행) | `/Users/kimjunwoo/Downloads/` → RAG 디렉토리 이동 예정 | 428p, 약 800 용어 | **공식(한국은행 발간물)** | ✅ 대상 |
| `주식용어모음집.pdf` | `/Users/kimjunwoo/Downloads/주식용어모음집.pdf` | 6p, 상황별 용어 | **확인 불가** — PDF 메타데이터 Producer=iLovePDF뿐(Title/Author 없음), 문서 내 URL·기관·저작권·발행처 표기 전무. 개인 자료 추정이나 근거 없음 | ❌ **제외** |

**결정: 출처·이용 근거를 확인할 수 없는 `주식용어모음집.pdf`는 전처리·적재 대상에서 제외한다.
Phase 4 용어 대상은 한국은행 경제금융용어 800선만 사용한다.**

### 800선 구조(항목 형식)
- 용어명(+영문 병기, 예 "결제(Settlement)") / 정의 단락 / "연관검색어" 줄.

- 파싱 시 컬럼 매핑(예정): 용어명 → `term`, 영문 병기 → `english_name`,
  정의 단락 → `official_definition`, "연관검색어" → `aliases`.
  (`easy_definition`은 800선에 없으므로 미채움 또는 향후 별도 생성.)
- 전처리 시 제거 대상: 초성 구분자(ㄱ/ㄴ/…), 페이지 번호·머리말, 목차(찾아보기) 페이지.
- SPEC(§4.1)이 규정한 `backend/scripts/index_financial_terms.py`는 **아직 존재하지 않음**
  (전처리·적재 스크립트 미작성).
- **DB 적재 현황: rag_terms 6건(시험용 시드)뿐. 위 사전 800+개는 아직 미적재.**
- 전체 용어사전 파싱·전처리·적재는 별도 작업으로 남는다(PDF → 용어/영문/정의/연관어 파싱 →
  rag_terms 적재). **지시에 따라 이번에는 진행하지 않는다.** 사용자가 원본을 RAG 디렉토리에
  넣어주면 그때 진행.

## 3-2. 용어사전 파싱 진행 상황 (PyMuPDF 좌표 + 목차 기준)

원본 PDF(`backend/docs/rag/2026_경제금융용어 800선.pdf`, git 제외)를 파싱하는 파서를 구현했다.
`scripts/parse_bok_terms.py`(파서), `scripts/verify_bok_sample.py`(20개 검증),
`scripts/dryrun_bok_terms.py`(789개 전체 드라이런). **DB 적재·임베딩 호출은 아직 안 함.**

### 20개 대표 검증 (`bok_verification.md`)
- 용어명/정의경계/연관검색어/source_page 각 20/20, 중복 0. 유형(약어·완전명·슬래시·연관多·법령·수식·페이지넘김) 고르게 포함.

### 789개 전체 드라이런 (`bok_dryrun_report.md`)
| 검사 | 결과 |
|---|---|
| canonical entry | **789** (목차 개념 782 + 슬래시 통합 반영) |
| 정규화 term 중복 | 0 |
| 정의 과단/과장/없음 | 0 / 0 / 0 |
| 머리말·꼬리말 잔여 | 0 |
| english_name 약어 오입력 | 0 |
| aliases↔related 중복 | 0 |
| 원문 대조(무작위50+이상치 69개) | 경계 69/69, 연관 69/69 |
| 페이지 넘는 정의 | 240건 정상 연결 |
| 기존 rag_terms 6건 충돌 | **0** (800선과 겹치는 term 없음) |

### 발견된 이상치 → **2차 수정 완료** (재드라이런 반영)

1. **수식/그래프/각주 오염 → 해결(관련어 혼입 0건).** 좌표·폰트 기반 일반 규칙으로 수정:
   - 그래프 축라벨(size<7.5) 제거, 진짜 연관검색어는 size 8.7~9.6 창(window)으로 한정.
   - related 수집을 **"연관검색어" 라벨을 실제로 만난 뒤에만** 시작 → 라벨 전 수식 변수는 정의 본문에 유지(유실 방지).
   - 제어문자·zero-width·불릿 정리. 원문 대조 50/50으로 정의 유실 없음 확인.
   - (핀테크·레그테크 등 related가 많은 건 실제 연관어가 많은 정상 항목.)
2. **source_page 14건 → 해결(페이지오류 0건).** 원인은 모두 목차 표기≠본문 제목:
   - 목차 영문 병기("연방준비은행(FRB)" vs 본문 "연방준비제도"), 슬래시 통합("동남아시아국가연합+한·중·일"),
     목차 미수록(4차 산업혁명·NFC·P-CBO·탄소국경세 등).
   - 수정: 목차 매핑 실패 시 `source_page = pdf_page - PAGE_OFFSET(18)`로 역산.
     **매핑 성공한 775건 전부 이 식이 정확히 성립함을 검증**했으므로 목차 매핑과 동일 규칙(하드코딩 아님).
3. **목차 미발견 7 / 미연결 18 → 항목별 분류(문서화, `bok_dryrun_report.md`)**:
   - 미발견 7건: 전부 목차 긴 용어명의 줄바꿈/표기 잔재.
   - 미연결 18건: 슬래시 통합 5건(의도) + 목차 표기 차이 13건(본문이 정본). 본문 파싱 오류 아님.

### 재드라이런 결과 (수정 후)
- canonical entry **789 유지**, 중복 0, 정의 과단/과장/없음 0
- **페이지오류 0, 수식 오염(related 혼입) 0, 노이즈 잔여 0**
- 원문 대조(무작위50+이상치 50개): **경계 50/50, 연관 50/50**
- 페이지 넘는 정의 244건, 기존 rag_terms 충돌 0
- **기존 테스트 108 passed**

### 전체 적재/임베딩 예상 (아직 미실행)
- 적재 예상 rows: **789** (기존 6건과 term 충돌 0 → 순증)
- passage 임베딩 예상 토큰: **~162,885** / 비용 **~$0.016** (참고 단가 $0.10/1M), 배치 8회
- 적재 시 필요: `rag_terms`에 `related_terms text[]` 컬럼 마이그레이션(GPT §3)

### 산출물 / Git
- 추적: `bok_verification.md`, `bok_dryrun_report.md`, 파서·검증·드라이런 스크립트
- 제외(gitignore): 원본 PDF, `bok_terms_sample20.json`, `bok_dryrun_full.json` (저작물 정의 원문)

## 3-3. 용어사전 전체 적재 완료 (789개)

`0019_rag_terms_extend.sql`(컬럼 추가) + `scripts/load_bok_terms.py`(적재).

### 스키마 확장 (0019, 기존 데이터 보존)
- rag_terms 에 추가: `related_terms text[]`, `source_name/title/edition`, `pdf_page`,
  `content_hash`, `embedding vector(1024)` + HNSW/content_hash 인덱스.
- 기존 시드 6건 컬럼만 추가되고 값 보존.

### 적재 결과
| 항목 | 값 |
|---|---|
| 적재(inserted) | **789** (embedded 789, failures 0) |
| 소요 | 18.1초 |
| 임베딩 실제 토큰 | ~162,885 |
| 임베딩 비용 | ~$0.0163 (참고 단가 $0.10/1M) |
| 총 rag_terms | **795** = 한국은행 789 + 시드 6 |
| 시드 6건 | 보존(덮어쓰기 0, source_name IS NULL 로 구분) |

- 저장 필드: term / english_name / official_definition / easy_definition=NULL /
  aliases / related_terms / source_name / source_title / source_edition / source_page /
  pdf_page / search_text / content_hash / embedding.
- **재실행 skip**: content_hash 로 789개 전부 skip, 추가 임베딩 0회·비용 $0 확인.
- **재시작 가능**: 배치(100개) 단위, 실패 배치만 격리(전체 재임베딩 안 함).
- 기존 term 충돌 0(시드 6건과 겹치는 term 없음). 충돌 시에도 시드는 skip 하도록 방어.

### 적재 후 검증 (`bok_load_verification.md`)
| 검사 | 결과 |
|---|---|
| 신규 canonical entry | **789** |
| 총 rag_terms | **795** (한국은행 789 + 시드 6, 실제 전후 6→795) |
| NULL 임베딩(신규) | **0** |
| term 중복 | **0** |
| official_definition 누락 | **0** |
| source_page 누락 | **0** |
| easy_definition 비-NULL | **0** (전부 NULL) |
| aliases↔related 혼입 | **0** |
| 무작위 50개 원문 대조 | 경계 50/50, source_page=pdf_page-18 규칙 50/50 |
| 검색 6종(정확·약어·영문명·슬래시·자연어×2) | **6/6 통과** |
| 벡터 검색(보조) | 정상(의미 유사 용어 회수 확인) |
| 기존 DART·재무 건수 | **무변경** (510/1524/4722/142) |
| 기존 테스트 / ruff | 108 passed / 통과 |

### 산출물 / Git (추가)
- 추적: `bok_load_verification.md`, `0019_rag_terms_extend.sql`(+rollback),
  `scripts/load_bok_terms.py`, `scripts/verify_bok_loaded.py`
- 임베딩은 solar-embedding-2-passage(1024), 뉴스 임베딩과 동일 세대(혼용 금지 원칙 준수)

## 4. 최소 통과 조건 판정
| 조건 | 판정 |
|---|---|
| 정확한 숫자를 문서 검색 결과에서 임의 선택하지 않음 | ✅ SQL fact 주입, numeric_sources에 원 단위 정확값 |
| 혼합 질문이 숫자 조회와 설명 검색을 모두 사용 | ✅ 병렬 실행 확인 |
| 전망값을 실제값처럼 표현하지 않음 | ✅ value_kind 라벨 + 프롬프트 규칙(현재 데이터는 전부 actual) |
| 정정 전 값을 최신값처럼 답하지 않음 | ✅ is_latest=true 우선 |

## 5. 테스트
- pytest **108 passed** (기존 95 + 신규 13: QueryPlan 8 + facts/format 5)
- ruff 통과
- FactsService 실환경 검증(재무 단위/기간/연결, 용어 정확·별칭, 정정 최신본)

## 6. 기존 데이터 영향
- DART·재무·공시 테이블은 **SELECT만**(수정/삭제 없음).
- 신규 쓰기는 rag_terms 용어 시드 6건뿐(별도 테이블, 기존 데이터 무관).

## 7. 기획서와 달라진 점
1. 금융용어는 **전체 사전 전처리 미완료** — 조회 구조만 구현하고 시험용 시드 6건만 적재
   (원본 사전 데이터 부재). SPEC의 `index_financial_terms.py`는 아직 없음.
2. 용어 후보 추출에 한국어 조사 접미사 제거 규칙 추가(특정 용어 하드코딩 아님).
3. 재무 account 매핑은 신호어→account_nm 일반 매핑(자주 묻는 항목부터, SPEC 유연 판단).

## 8. 아직 남은 문제 / 유의
- 단독 숫자 질문("영업이익 얼마?")에 여러 기간 fact(12건)를 함께 줘 LLM이 특정 기간 라벨을
  가끔 혼동. numeric_sources엔 정확값이 다 담기므로 최소통과조건엔 영향 없음.
  개선안: 단독 질문 시 최신 1건만 강조 주입(추후 튜닝).
- 별도재무·2023 이전·주가는 데이터/범위상 미지원(조사에 명시).
- **용어 6개는 시험용 시드. 전체 용어사전 전처리·적재는 미완료**(원본 데이터 부재).
  운영/발표 전 원본 사전 확보 후 적재 필요.

## 9. 사용자 확인이 필요한 것
- 없음 (기존 데이터 무변경, 비용/보안 위험 없음)

## 10. 다음 Phase
- Phase 5(증권사 리포트) — 지시대로 자동 진행하지 않고 대기.

---

## Phase 종료 기록 (계획서 반영용)
```text
상태: 완료
완료일: 2026-07-22
지원하는 숫자 항목: 매출액·영업이익·당기순이익·자산/부채/자본총계·영업/투자/재무활동현금흐름 (연결, 원 단위)
지원하는 공시 유형: 정기보고서·주요사항(구조화 값+요약), 정정공시 최신본, 일정성 이벤트
등록된 용어 수: 6 (ADR·영업이익·당기순이익·유상증자·자기주식·정정공시)
혼합 질문 결과: 숫자 SQL + 설명 RAG 병렬 사용, 인용 오류 0, 필수 질문 5개 통과
기획서와 달라진 점: 용어는 소량 시드로 시작, 조사 접미사 제거 규칙 추가
남은 지원 범위: 별도재무·2023 이전·주가(Phase6) 미지원, 단독 숫자질문 기간 표기 튜닝
```
