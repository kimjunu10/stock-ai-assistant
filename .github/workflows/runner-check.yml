name: Runner Check

on:
  workflow_dispatch:
  push:
    branches: ["feat/news-backfill"]

permissions:
  contents: read

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner picked up the job"
